# 项目文件结构说明

本文档记录当前仓库主要文件职责。当前项目主线是 **已有账户 Plus 新流程**，旧注册流程和多注册迁移文档已经移除。

不纳入本清单的目录：

- `.git/`
- `.github/`
- `.vscode/`
- `_metadata/`
- `docs/images/`
- `icons/`

## 根目录

- `.gitignore`：仓库忽略规则。
- `LICENSE`：项目许可证。
- `README.md`：面向使用者的中文介绍、安装和当前流程说明。
- `SECURITY.md`：安全说明。
- `CONTRIBUTING.md`：贡献说明。
- `RELEASING.md`：发布说明。
- `THIRD_PARTY_NOTICES.md`：第三方来源声明。
- `manifest.json`：Chrome 扩展清单，声明后台、侧边栏、内容脚本、权限和静态规则。
- `background.js`：后台 Service Worker 入口，负责模块装配、状态管理、消息路由接入、自动运行和步骤执行器注册。
- `package.json`：仓库最小 Node 配置。
- `rules.json`：静态请求规则。
- `ZeroOmegaOptions.bak`：代理工具配置备份。
- `start-hotmail-helper.bat`：Windows 本地 helper 启动脚本。
- `start-hotmail-helper.command`：macOS 本地 helper 启动脚本。
- `项目完整链路说明.md`：当前已有账户 Plus 主流程说明。
- `项目文件结构说明.md`：当前文件。
- `项目开发规范（AI协作）.md`：AI 协作开发规范。
- `开发者AI开发与PR提交流程.md`：开发者使用 AI 提交 PR 的流程说明。
- `mihomo-yaml-prompt.md`：代理配置生成提示词。

## 根目录工具模块

- `cloudflare-temp-email-utils.js`：Cloudflare Temp Email 数据归一化工具。
- `cloudmail-utils.js`：Cloud Mail / SkyMail API、域名、邮件正文归一化工具。
- `gopay-utils.js`：GoPay / GPC 支付字段、接口路径和响应归一化工具。
- `hotmail-utils.js`：Hotmail 账号筛选、验证码匹配和接口响应归一化工具。
- `icloud-utils.js`：iCloud 隐私邮箱列表、保留状态和已用状态归一化工具。
- `luckmail-utils.js`：LuckMail 邮箱购买记录、邮件 cursor 和验证码匹配工具。
- `mail-provider-utils.js`：网页邮箱 provider 配置和入口归一化工具。
- `mail2925-utils.js`：2925 账号池解析、冷却期和可用账号挑选工具。
- `managed-alias-utils.js`：Gmail / 2925 别名邮箱规则工具。
- `microsoft-email.js`：Microsoft Graph 邮件读取辅助模块。
- `paypal-utils.js`：PayPal 账号池归一化和查询工具。

## `shared/`

- `shared/existing-account.js`：解析和校验账户 JSON，构建已有账户运行态。
- `shared/flow-capabilities.js`：当前 flow 能力矩阵；固定 Plus 模式，关闭手机号注册、贡献模式和多面板切换。
- `shared/session-to-json-converter.js`：把 ChatGPT 会话结构转换为本地 JSON 的工具。
- `shared/source-registry.js`：来源页、内容脚本驱动和清理范围注册表。

## `data/`

- `data/step-definitions.js`：当前工作流步骤定义。PayPal Hosted 默认 7 步，GoPay / GPC 按支付方式替换中段步骤。
- `data/address-sources.js`：Checkout 账单地址候选数据。
- `data/names.js`：姓名数据。

## `background/`

- `background/account-run-history.js`：账号运行记录模块。
- `background/auto-run-controller.js`：自动运行、多轮执行、停止恢复和失败处理控制器。
- `background/cloudmail-provider.js`：Cloud Mail / SkyMail 邮件读取和邮箱生成 provider。
- `background/contribution-oauth.js`：历史贡献接口适配模块；当前侧边栏主流程不暴露贡献模式。
- `background/cpa-api.js`：CPA 管理接口辅助模块，保留给兼容导入能力使用。
- `background/generated-email-helpers.js`：邮箱生成辅助模块，保留给兼容验证码和邮箱能力使用。
- `background/ip-proxy-core.js`：IP 代理解析、PAC 应用、鉴权和出口检测核心。
- `background/ip-proxy-provider-711proxy.js`：711Proxy 参数解析和账号转换规则。
- `background/local-cli-proxy-api.js`：本地 OAuth 与本地 JSON 文件生成能力。
- `background/logging-status.js`：结构化日志、状态和错误判断工具。
- `background/mail-2925-session.js`：2925 会话、登录和账号池冷却管理。
- `background/mail-rule-registry.js`：邮件规则注册表。
- `background/message-router.js`：后台 runtime message 路由。
- `background/navigation-utils.js`：URL、来源页、回调和面板模式判断工具。
- `background/panel-bridge.js`：面板桥接兼容层。
- `background/paypal-account-store.js`：PayPal 账号池持久化模块。
- `background/phone-verification-flow.js`：历史手机号验证兼容模块；当前主流程不开放手机号注册。
- `background/plus-success-session-upload.js`：Plus 成功页会话读取与后续处理辅助模块。
- `background/registration-email-state.js`：历史注册邮箱运行态兼容模块。
- `background/runtime-state.js`：运行态分组、扁平字段兼容和默认状态构建。
- `background/signup-flow-helpers.js`：历史认证页辅助模块。
- `background/sub2api-api.js`：SUB2API 管理接口兼容模块。
- `background/tab-runtime.js`：标签页注册、创建、注入、等待和消息超时基础设施。
- `background/verification-flow.js`：邮箱验证码轮询和提交兼容层。
- `background/workflow-engine.js`：节点工作流读取、节点状态和下一节点解析。

## `background/steps/`

- `background/steps/open-chatgpt.js`：步骤 1，打开 ChatGPT 官网。
- `background/steps/existing-account-login.js`：步骤 2，使用已有账号邮箱和密码登录。
- `background/steps/fetch-existing-login-code.js`：步骤 3，从账户 JSON 的 `mailbox_url` 获取并提交登录验证码。
- `background/steps/create-plus-checkout.js`：步骤 4，以及 PayPal Hosted 第 5、6 步的主要执行器。
- `background/steps/fill-plus-checkout.js`：传统 Checkout / GPC 任务等待执行器。
- `background/steps/gopay-manual-confirm.js`：GoPay / GPC 手动确认执行器。
- `background/steps/gopay-approve.js`：GoPay 授权页执行器。
- `background/steps/paypal-approve.js`：PayPal 授权兼容执行器。
- `background/steps/plus-return-confirm.js`：支付后回跳确认兼容执行器。
- `background/steps/plus-activation-success.js`：最终 Plus 开通成功节点。
- `background/steps/cpa-session-import.js`：CPA 会话导入兼容执行器。
- `background/steps/sub2api-session-import.js`：SUB2API 会话导入兼容执行器。
- `background/steps/registry.js`：节点执行注册表。

## `content/`

- `content/activation-utils.js`：页面按钮激活和可恢复错误判断工具。
- `content/auth-page-recovery.js`：认证页重试页恢复工具。
- `content/signup-page.js`：OpenAI 认证页内容脚本，承接已有账户登录和验证码提交。
- `content/plus-checkout.js`：Checkout 创建、Hosted 页面填写和会话读取脚本。
- `content/paypal-flow.js`：PayPal Hosted / PayPal 授权页面脚本。
- `content/gopay-flow.js`：GoPay 页面脚本。
- `content/operation-delay.js`：页面操作间延迟工具。
- `content/utils.js`：内容脚本通用工具。
- `content/phone-auth.js`、`content/phone-country-utils.js`：手机号验证兼容工具。
- `content/duck-mail.js`、`content/gmail-mail.js`、`content/icloud-mail.js`、`content/inbucket-mail.js`、`content/mail-163.js`、`content/mail-2925.js`、`content/qq-mail.js`：历史邮箱读取脚本，保留给辅助验证码能力使用。
- `content/sub2api-panel.js`、`content/vps-panel.js`：面板兼容脚本。
- `content/whatsapp-flow.js`：WhatsApp OTP 兼容脚本。

## `flows/`

- `flows/openai/mail-rules.js`：OpenAI 邮件验证码匹配规则。

## `phone-sms/`

- `phone-sms/providers/hero-sms.js`：HeroSMS provider 适配层。
- `phone-sms/providers/five-sim.js`：5sim provider 适配层。
- `phone-sms/providers/registry.js`：接码 provider 注册表。

## `sidepanel/`

- `sidepanel/sidepanel.html`：侧边栏页面结构，当前入口是账户 JSON、Plus 支付、自动运行和日志。
- `sidepanel/sidepanel.js`：侧边栏主逻辑，负责配置收集、保存、步骤渲染和自动运行启动。
- `sidepanel/sidepanel.css`：侧边栏样式。
- `sidepanel/account-records-manager.js`：账号记录覆盖层。
- `sidepanel/hosted-sms-pool-manager.js`：PayPal Hosted 接码池管理。
- `sidepanel/ip-proxy-panel.js`：IP 代理配置和状态展示。
- `sidepanel/ip-proxy-provider-711proxy.js`：711Proxy 输入辅助。
- `sidepanel/editable-list-picker.js`：可编辑列表选择器。
- `sidepanel/form-dialog.js`：公共表单弹窗。
- `sidepanel/paypal-manager.js`：PayPal 账号池兼容管理器。
- `sidepanel/account-pool-ui.js`、`sidepanel/custom-email-pool-manager.js`、`sidepanel/hotmail-manager.js`、`sidepanel/icloud-manager.js`、`sidepanel/luckmail-manager.js`、`sidepanel/mail-2925-manager.js`：历史邮箱和号池 UI 管理器，当前主流程不作为入口使用。
- `sidepanel/contribution-content-update-service.js`、`sidepanel/contribution-mode.js`：历史贡献内容和贡献模式 UI 兼容模块。
- `sidepanel/update-service.js`：GitHub Releases 更新检查服务。

## `scripts/`

- `scripts/gpc_sms_helper_macos.py`：GPC 本地短信 OTP helper。
- `scripts/hotmail_helper.py`：本地 helper 服务，负责 Hotmail 读取和账号记录快照同步。

## `services/checkout-converter/`

- `services/checkout-converter/README.md`：支付转换服务中文说明。
- `services/checkout-converter/app.py`：支付转换服务主程序。
- `services/checkout-converter/requirements.txt`：Python 依赖列表。
- `services/checkout-converter/Dockerfile`：容器构建文件。

## `docs/`

- `docs/使用教程/使用教程.md`：中文教程索引。
- `docs/使用教程/使用教程书写模板.md`：教程写作模板。
- `docs/使用教程/分部分/*.md`：分主题教程正文。
- `docs/ip-proxy-module.md`：IP 代理模块说明。
- `docs/local-customizations-maintenance.md`：本地定制维护说明。
- `docs/错误重试分层策略.md`：错误重试层级说明。
- `docs/公告书写标准.md`：公告文案标准。
- `docs/仓库协作者AI分析PR与合并标准流程.md`：协作者 PR 审查与合并流程。
- `docs/releases/v0.1.2.md`、`docs/releases/v0.1.3.md`：版本发布说明。

## `tests/`

- `tests/auto-run-existing-account-flow.test.js`：已有账户自动运行流程能力测试。
- `tests/background-existing-account-settings.test.js`：已有账户设置归一化测试。
- `tests/existing-account-login-registration.test.js`：已有账户登录步骤注册测试。
- `tests/existing-account.test.js`：账户 JSON 解析与校验测试。
- `tests/fetch-existing-login-code.test.js`：登录验证码获取步骤测试。
- `tests/paypal-hosted-billing-review.test.js`：PayPal Hosted 账单确认识别测试。
- `tests/paypal-hosted-generic-error-logout.test.js`：PayPal Hosted 错误态登出恢复测试。
- `tests/plus-activation-success.test.js`：Plus 开通成功节点测试。
- `tests/plus-checkout-cloud-retry-classification.test.js`：云端支付转换重试分类测试。
- `tests/plus-checkout-hosted-split.test.js`：Hosted Checkout 步骤拆分测试。
- `tests/plus-checkout-payurl-api.test.js`：支付转换 API 接入测试。
- `tests/sidepanel-existing-account-ui.test.js`：侧边栏已有账户 UI 测试。
- `tests/step-definitions-existing-account.test.js`：当前步骤定义测试。
