# 项目完整链路说明

本文面向开发者和 AI 协作者，用于说明当前 GuJumpgate 的真实运行链路。本文只描述现存主流程：**已有账户 Plus 新流程**。

旧的自动注册 Free 账号、手机号注册、多注册迁移设计、独立贡献 OAuth 小流程以及 SUB2API / CPA / VPS 旧面板兼容入口已经删除。遇到旧文档或旧步骤名时，应以当前代码中的 `data/step-definitions.js`、`shared/flow-capabilities.js` 和侧边栏界面为准。

## 1. 项目目标

GuJumpgate 是一个 Chrome 扩展，目标是把下面这些动作串成可恢复、可观察的自动化流程：

1. 读取用户提供的已有账户 JSON。
2. 打开 ChatGPT 首页。
3. 进入 OpenAI 认证页并登录已有账号。
4. 通过用户配置的验证码接口获取登录验证码。
5. 创建 Plus Checkout。
6. 完成 PayPal Hosted 支付链路。
7. 确认 Plus 开通成功。

当前流程必须在全局美国节点前提下运行。自动运行前应确认浏览器访问 ChatGPT、OpenAI 认证页、PayPal 和支付转换链路时都走美国出口；规则分流、局部代理或非美国节点都不属于当前支持运行环境。

当前流程默认 `activeFlowId = openai`，`Plus 模式` 固定开启，`panelMode` 固定为 `local-cpa-json`。侧边栏不再给用户暴露旧注册入口和旧流程切换入口。

## 2. 当前步骤定义

步骤元数据由 [data/step-definitions.js](data/step-definitions.js) 生成，后台执行器由 [background/steps/registry.js](background/steps/registry.js) 注册。

当前支持范围只包含 PayPal Hosted Checkout。GoPay 和 GPC 相关代码如果仍存在，只视为历史兼容或待清理内容，不属于现行可用流程。

默认 PayPal 路径使用 Hosted Checkout 分段节点：

| 步骤 | 节点 | 说明 |
|---|---|---|
| 1 | `open-chatgpt` | 打开 ChatGPT 官网并准备会话页面。 |
| 2 | `existing-account-login` | 使用账户 JSON 中的邮箱和密码登录已有账号。 |
| 3 | `fetch-existing-login-code` | 从 `mailbox_url` 获取登录验证码并提交。 |
| 4 | `plus-checkout-create` | 读取登录会话，创建 Plus Checkout。 |
| 5 | `hosted-checkout-submit` | 在 Hosted Checkout 页面填写账单和必要验证码。 |
| 6 | `hosted-paypal-payment` | 处理 PayPal Hosted 支付页。 |
| 7 | `plus-activation-success` | 写入 Plus 开通成功状态。 |

## 3. 核心参与者

### 3.1 侧边栏

[sidepanel/sidepanel.html](sidepanel/sidepanel.html) 和 [sidepanel/sidepanel.js](sidepanel/sidepanel.js) 负责：

- 展示账户 JSON、PayPal Hosted、自动运行和日志配置。
- 固定写入 `activeFlowId = openai`、`panelMode = local-cpa-json`、`plusModeEnabled = true`。
- 校验账户 JSON 必须包含 `email`、`password`、`mailbox_url`。
- 渲染 PayPal Hosted 当前步骤。
- 保存运行配置并发送 `AUTO_RUN` 消息。
- 展示账号运行记录、更新提示和步骤状态。

### 3.2 后台 Service Worker

[background.js](background.js) 是后台入口，负责加载共享模块、后台模块、步骤执行器和内容脚本工具。它的核心职责是：

- 管理状态和自动运行会话。
- 根据当前 `state` 选择步骤注册表。
- 执行节点并等待完成信号。
- 维护日志、重试、停止和恢复逻辑。
- 协调标签页创建、内容脚本注入、消息路由和支付页状态。

### 3.3 内容脚本

`content/` 下脚本负责页面内 DOM 操作：

- [content/signup-page.js](content/signup-page.js)：认证页登录、验证码提交和状态识别。
- [content/plus-checkout.js](content/plus-checkout.js)：Checkout 页面创建、Hosted 页面填写、会话读取。
- [content/paypal-flow.js](content/paypal-flow.js)：PayPal Hosted 支付页识别与提交。

### 3.4 共享模块

- [shared/existing-account.js](shared/existing-account.js)：账户 JSON 解析、校验和运行态构建。
- [shared/flow-capabilities.js](shared/flow-capabilities.js)：当前 flow 能力矩阵；它锁定 Plus 模式、关闭手机号注册和贡献模式。
- [shared/source-registry.js](shared/source-registry.js)：来源页、驱动和清理范围注册。
- [shared/session-to-json-converter.js](shared/session-to-json-converter.js)：会话 JSON 转换工具，供本地导出能力复用。

## 4. 配置与状态

### 4.1 持久配置

持久配置主要写入 `chrome.storage.local`：

- `existingAccountJson`
- `plusPaymentMethod`
- PayPal Hosted 相关配置：云端转换开关、本地转换代理、验证码接口、号码池、弹窗延迟、成功后延迟
- 自动运行设置：失败跳过、步间延迟、操作间延迟
- 账号运行历史

### 4.2 运行态

运行态主要写入 `chrome.storage.session`：

- 当前 flow、当前节点、节点状态
- 自动运行会话 ID、轮次、目标次数
- 当前标签页注册表和最近来源地址
- 当前 Plus Checkout 标签、链接、PayPal 支付任务状态
- 当前登录账号邮箱、密码和统一账号标识
- 日志和步骤状态

[background/runtime-state.js](background/runtime-state.js) 负责在扁平状态和 flow 分组状态之间同步，避免旧字段污染当前新流程。

## 5. 自动运行链路

自动运行入口在侧边栏：

1. 校验 `账户 JSON`。
2. 强制保存当前配置。
3. 确认用户已在全局美国节点环境中运行。
4. 发送 `AUTO_RUN` 消息。
5. 后台创建新的 `autoRunSessionId`。
6. 按工作流节点顺序执行。
7. 节点失败时根据支付场景判断是否重建 Checkout、重试当前节点或结束当前轮。
8. 用户点击停止后，旧倒计时、旧恢复入口和旧重试链路都不能重新拉起已经失效的自动运行。

后台日志使用结构化 `nodeId / stepKey / step` 元数据。日志正文只做展示，不再作为步骤号解析来源。

## 6. 支付链路边界

PayPal 默认走 Hosted Checkout：

- 第 4 步创建 Checkout。
- 第 5 步处理 Hosted Checkout 上的账单、验证码和提交。
- 第 6 步处理 PayPal Hosted 支付页。
- 成功回到 ChatGPT 支付成功页后进入 Plus 成功确认。

云端支付转换打开时，本地支付转换代理不会生效。关闭云端转换后，才会使用本地页面和本地转换代理。

## 7. 已删除能力

以下内容已经从当前主线入口中删除：

- 自动注册 Free 账号流程。
- 手机号注册流程。
- 多注册 flow 迁移设计文档。
- 贡献模式作为用户可选主流程。
- CPA / SUB2API / Codex2API 作为侧边栏可切换目标面板。
- GoPay / GPC 作为当前支持支付链路。

新增文档、README 和使用教程都应以已有账户 Plus 新流程为准。
